---
- name: 'Prepare'
  hosts: 'all'
  gather_facts: false
  tasks:
    - name: 'Prepare | inject | add packages to be removed'
      ansible.builtin.apt:
        name:
          - 'git'
          - 'curl'
          - 'git-lfs'
        state: 'present'
